25project01 팀 프로젝트 환경설정 가이드

*필수
.gitignore
25project01/


📋 설치 요구사항
Python 3.10
TensorFlow 2.10
Jupyter Notebook(anaconda3)
cudatoolkit=11.2 
cudnn=8.1.0
numpy=1.21.6
pandas=1.5.3
matplotlib=3.6.3
seaborn=0.12.2
jupyter=1.0.0
jupyterlab=3.6.1
ipykernel=6.21.2
flask=2.2.3
cx-Oracle=8.3.0
sqlite3  # Python 내장 모듈이지만 확인차
scikit-learn=1.2.1
scipy=1.10.0
requests=2.28.2
openpyxl=3.1.1
xlrd=2.0.1

기타 데이터 분석 및 웹 개발 라이브러리

🔧 1단계: Python 3.10 설치 확인
*Windows 사용자
Python 공식 웹사이트에서 Python 3.10.x 다운로드
https://www.python.org/downloads/release/python-3100/
설치 시 "Add Python to PATH" 체크박스 반드시 선택
설치 완료 후 명령 프롬프트(cmd)에서 확인:
bash
python --version

*macOS 사용자
bash
# Homebrew를 사용한 설치 (권장)
brew install python@3.10
# 또는 pyenv 사용
brew install pyenv
pyenv install 3.10.12
pyenv global 3.10.12

*Linux (Ubuntu/Debian) 사용자
bash
sudo apt update
sudo apt install python3.10 python3.10-venv python3.10-dev python3-pip


🌐 2단계: 가상환경 생성 및 활성화
가상환경 생성
bash
# 프로젝트 폴더로 이동 (원하는 위치에)
cd C:\어쩌구저쩌구
저는 cd C:\ai_x\ideanote\250617_1팀프로젝트

# 가상환경 생성
python -m venv 25project01

가상환경 활성화
*Windows:
bash
25project01\Scripts\activate

*macOS/Linux:
bash
source 25project01/bin/activate

✅ 성공 확인: 터미널 앞부분에 (25project01)이 표시되면 성공


🚀 GPU 설정 (선택사항 - 성능 향상)
Windows 사용자 - GPU 설정
중요: Windows Native에서는 TensorFlow 2.10이 GPU를 지원하는 마지막 버전입니다.

1단계: NVIDIA GPU 드라이버 설치
NVIDIA 드라이버 다운로드 에서 최신 드라이버 설치
설치 후 명령 프롬프트에서 확인:
bash
nvidia-smi

2단계: CUDA 및 cuDNN 설치 (conda 권장)
Anaconda3 설치 후:
패스 등록 
C:\Users\User\anaconda3\Scripts (저는 컴터이름이 User라서 PC명으로 바꾸세요)
내PC우클릭 속성->고급시스템설정->환경변수->Path더블클릭->새로만들기
C:\Users\User\anaconda3\Scripts
C:\Users\User\anaconda3
이렇게 두개 Path에 등록하시는데 User(PC명 확인하세요)
귀찮으면 이부분은 콘다프롬프트로하셔도되요 저는 cmd로해서
패스 2개 등록 하신 후에 cmd 창을 완전히 껐다가 다시 실행하시면 conda명령어가 가능해집니다.

bash
# 가상환경에서 CUDA와 cuDNN 설치
conda install -c conda-forge cudatoolkit=11.2 cudnn=8.1.0
3단계: TensorFlow GPU 버전 설치
bash
# TensorFlow 2.10 (Windows에서 GPU 지원하는 마지막 버전)
pip install "tensorflow<2.11"

*macOS: 공식 GPU 지원 없음 (CPU만 사용) 

*Linux:
bash
# 최신 TensorFlow with GPU
pip install tensorflow[and-cuda]


📦 3단계: 필수 라이브러리 설치
pip 업그레이드
bash
python -m pip install --upgrade pip
핵심 라이브러리 설치 (순서 중요!)

*라이브러리 설치
requirements.txt파일을 처음 접속한 폴더경로에 넣고
(가상환경에서 저는 cd C:\ai_x\ideanote\250617_1팀프로젝트 여기)
아래 명령어 치면 한번에 라이브러리 설치
pip install -r requirements.txt


*혹은 한줄씩 설치
(한번에 설치하셨으면 할필요없음 아래 Oracle 데이터베이스 설정으로 가세요)
bash
# 1. 기본 계산 라이브러리
pip install numpy==1.21.6
pip install pandas==1.5.3
# 2. 시각화 라이브러리
pip install matplotlib==3.6.3
pip install seaborn==0.12.2
# 4. Jupyter 환경
pip install jupyter==1.0.0
pip install jupyterlab==3.6.1
pip install ipykernel==6.21.2
# 5. 웹 프레임워크
pip install flask==2.2.3
# 6. 데이터베이스 연결
pip install cx-Oracle==8.3.0
* sqlite3(파이썬 표준 라이브러리에 포함되어 있어서 별도 인스톨없음)
# 7. 기타 유용한 라이브러리
pip install scikit-learn==1.2.1
pip install scipy==1.10.0
pip install requests==2.28.2
(리퀘스트 설치 에러표시는 신경쓸필요없음:주피터랩이 최신requests버전요구하는에러)
pip install openpyxl==3.1.1
pip install xlrd==2.0.1


🔌 4단계: Oracle 데이터베이스 설정
Oracle 11g XE 설치 후
시스템 변수에 추가:
ORACLE_HOME
C:\oraclexe\app\oracle\product\11.2.0\server

PATH에 추가: 
%ORACLE_HOME%\bin

*macOS:
bash
brew install instantclient-basic

*Linux:
bash
# Ubuntu/Debian
sudo apt-get install libaio1
# Oracle Instant Client RPM 또는 ZIP 다운로드 후 설치


📓 5단계: Jupyter Notebook 설정
Jupyter 커널 등록
bash
python -m ipykernel install --user --name=25project01 --display-name "25project01"

*시작경로 변경된 Jupyter Notebook 바로가기 만들기
C:\Users\User\anaconda3\Scripts\jupyter-notebook.exe --notebook-dir="C:/ai_x/ideanote/250617_1팀프로젝트"
(이렇게 주피터 노트북 시작위치 할곳으로 바로가기 만들때 입력하신 후에
바로가기 우클릭 하셔서 대상 주소를 아래처럼 바꿔주세요)
cmd /c "C:\ai_x\ideanote\250617_1팀프로젝트\25project01\Scripts\activate && jupyter notebook --notebook-dir=C:\ai_x\ideanote\250617_1팀프로젝트"


*CUDA_PATH 환경변수 생성
1.변수 이름: CUDA_PATH
변수 값: C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v11.2
2. PATH에 bin 경로 추가
기존 PATH에 추가: %CUDA_PATH%\bin



저는 쿠다가 문제가 있었어서
제어판에서 11.8dev 제거하고 아나콘다프롬프트에서 가상환경 들어가서
25project01\Scripts\activate
conda activate 25project01
conda install -c conda-forge cudatoolkit=11.2 cudnn=8.1.0
(저는 Path에 C:\Users\User\anaconda3\Library\bin 이것도 추가해야 되더라고요(저는 설치 경로가 저기에 되있어서))
python gpu_test.py
로 확인했습니다(압축파일에 넣어둠 혹은 아래 코드로 작성후 확인 하셔요)


*GPU 사용 확인 스크립트
다음 코드를 gpu_test.py 파일로 저장하고 실행:

import tensorflow as tf
print(f"TensorFlow 버전: {tf.__version__}")
print(f"GPU 사용 가능: {tf.config.list_physical_devices('GPU')}")

# GPU 장치 상세 정보
gpus = tf.config.list_physical_devices('GPU')
if gpus:
    print(f"🎉 GPU 발견: {len(gpus)}개")
    for i, gpu in enumerate(gpus):
        print(f"  GPU {i}: {gpu}")
    
    # GPU 메모리 증가 설정 (권장)
    try:
        for gpu in gpus:
            tf.config.experimental.set_memory_growth(gpu, True)
        print("✅ GPU 메모리 증가 설정 완료")
    except RuntimeError as e:
        print(f"⚠️ GPU 설정 주의: {e}")
else:
    print("❌ GPU가 발견되지 않았습니다. CPU 모드로 실행됩니다.")




*가상환경 활성화 (매번 작업 시작 시)
cd C:\ai_x\ideanote\250617_1팀프로젝트
25project01\Scripts\activate

# macOS/Linux
source 25project01/bin/activate

*가상환경 비활성화
bash
deactivate

*설치된 패키지 목록 확인
bash
pip list


*Git 사용 시: .gitignore에 25project01/ 폴더 추가


